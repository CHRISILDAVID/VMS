# UI Fixes:
- 30 min slots
- In case of overlap in bookings "Restrict the new incoming booking and Redirect the user(owner) back to the schedule screen with an error message like "Booking Conflict: Slot already booked" and also highlight the conflicting slot in the schedule."
- On creating new membership batch(With members), the payments page shows the new membership batch, But not the members in the batch and the payment details of that batch.
- Number verification only, Owner account creation and venue mapping only through admin portal.
- Owners assigned to venues only via admin portal, want to know how every the test account can see all venues.