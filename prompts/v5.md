# prompt
Update ONLY the Members and Payments pages.

Do NOT modify, redesign, or change any other page in the app, including:

- Schedule
- Bookings
- Profile
- Bottom Navigation
- Design System
- Components
- Colors
- Typography
- Icons
- Spacing
- Auto Layout

Keep the existing UI exactly as it is. Only implement the following changes in the Members and Payments pages while maintaining the current design language and reusing existing components wherever possible.
Keep the existing Members page design exactly the same. Do not change the UI, layout, colors, spacing, or components. Only add the following features. 

1. Create Membership Slot Add a new section called "Initial Members". Include: • + Add Member button • Fields: Name and Mobile Number • Allow adding multiple members • Edit and Remove actions • Show member count (e.g. 3/6 Members Added) The owner should be able to publish the slot with or without members.

2. Enable the existing "Members" button on each Membership Slot card.

When the owner taps "Members", open a Members List for that slot.

Display each member in a card with:
- Name
- Mobile Number
- Payment Status

In the same row, add an Active Status toggle (Active / Inactive).

Actions for each member:
- Edit Member
- Transfer to Another Slot
- Remove Member

If the owner switches the toggle to Inactive, the member remains in the list with an "Inactive" badge and stops receiving future membership payment requests until reactivated.

Add a floating "+ Add Member" button.

The Add Member form should ask only:
- Name
- Mobile Number
Allow the owner to add multiple members to the slot. 

3. Edit Membership Slot Enable the existing Edit button. Allow editing: - Slot Name - Playing Days - Start & End Time - Skill Level - Monthly Fee - Capacity - Guest Play Fee - Allow Guest Play - Open/Close Recruitment Do not manage members from this page. Member management should happen only through the Members screen.
Update the Payments flow to match the Membership Slots structure. Do not redesign the UI—reuse the existing design system, components, spacing, colors, and payment cards.

Current Flow:
Payments → List of all members

Replace it with:

Payments
↓
Dashboard Cards (keep existing)
↓
Membership Slots List
↓
View Payments
↓
Member Payments

----------------------------------------

Membership Slots

Each slot card should display:

- Slot Name
- Playing Days
- Time
- Skill Level
- Monthly Fee
- Total Members
- Paid Members
- Pending Members
- Expected Collection
- Collected Amount
- Pending Amount

Example:

Morning Warriors
Mon, Wed, Fri • 6:00–7:00 PM

6 Members
₹15,000 Expected
₹12,500 Collected
₹2,500 Pending

Button:
[View Payments]

----------------------------------------

When "View Payments" is tapped, open a new screen.

Header:

- Slot Name
- Playing Days
- Time

Summary Cards:

- Total Members
- Paid
- Pending
- Overdue
- Expected Collection
- Collected Amount

Below the summary, add filter chips:

All | Paid | Pending | Overdue

----------------------------------------

Member Payment List

Reuse the existing payment card design.

Each card should display:

- Member Name
- Mobile Number
- Monthly Fee
- Payment Status
- Due Date
- Payment Method (if paid)

Actions:

- Mark as Paid
- Send Reminder
- View History
- Download Receipt

Status:

- Paid
- Due Soon
- Pending
- Overdue

Keep the existing color coding.

----------------------------------------

Do not change the visual design. Only change the navigation flow from:

Payments → Members

to

Payments → Membership Slots → View Payments → Member Payments.

Reuse existing components wherever possible and maintain the current premium mobile UI.

# Response
Let me read both screens first to understand the exact current state.

Read 2 files
Wrote
Wrote
Updating MembersScreen
Update PaymentsScreen: Slot cards → View Payments drill-down flow
Build check
Clean build. Both screens are done:

Members screen — new features wired up:

CreateSlotSheet now has an "Initial Members" section with + Add Member, name/phone form, edit/remove per member, capacity progress bar, and the Publish button showing member count
"Members" button on each slot card → SlotMembersView with member cards (Name, Mobile, Payment Status, Active/Inactive toggle), Edit / Transfer / Remove actions, floating + Add Member FAB, and transfer flow between slots
"Edit" button → EditSlotSheet with all slot fields (Name, Time, Capacity, Fee, Guest Fee, Skill, Days, Allow Guest, Open Recruitment) — no member management
Payments screen — full drill-down structure:

Main screen: existing KPI dashboard cards kept at top, then Membership Slot cards with Total/Paid/Pending members, Expected/Collected/Pending amounts, collection progress bar, and [View Payments] button
"View Payments" → SlotPaymentsScreen with slot header, dual KPI rows, filter chips (All / Paid / Pending / Overdue), and reused payment card design with Mark Paid, Remind, History, Download Receipt actions