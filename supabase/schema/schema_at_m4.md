-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.owners (
  id uuid NOT NULL,
  full_name text NOT NULL,
  phone text NOT NULL UNIQUE,
  email text,
  avatar_url text,
  business_name text NOT NULL,
  role USER-DEFINED NOT NULL DEFAULT 'owner'::user_role,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT owners_pkey PRIMARY KEY (id),
  CONSTRAINT owners_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.venues (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  owner_id uuid NOT NULL,
  name text NOT NULL,
  address text,
  city text,
  state text,
  pincode text,
  latitude numeric,
  longitude numeric,
  contact_phone text,
  contact_email text,
  court_type USER-DEFINED,
  amenities ARRAY DEFAULT '{}'::text[],
  photos ARRAY DEFAULT '{}'::text[],
  gstin text,
  gst_enabled boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT venues_pkey PRIMARY KEY (id),
  CONSTRAINT venues_owner_id_fkey FOREIGN KEY (owner_id) REFERENCES public.owners(id)
);
CREATE TABLE public.courts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  venue_id uuid NOT NULL,
  name text NOT NULL,
  court_type USER-DEFINED,
  sort_order integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT courts_pkey PRIMARY KEY (id),
  CONSTRAINT courts_venue_id_fkey FOREIGN KEY (venue_id) REFERENCES public.venues(id)
);
CREATE TABLE public.operating_schedules (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  venue_id uuid NOT NULL,
  day_of_week USER-DEFINED NOT NULL,
  is_closed boolean NOT NULL DEFAULT false,
  is_24h boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT operating_schedules_pkey PRIMARY KEY (id),
  CONSTRAINT operating_schedules_venue_id_fkey FOREIGN KEY (venue_id) REFERENCES public.venues(id)
);
CREATE TABLE public.pricing_blocks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  schedule_id uuid NOT NULL,
  start_time time without time zone NOT NULL,
  end_time time without time zone NOT NULL,
  price_per_hour integer NOT NULL,
  court_ids ARRAY DEFAULT '{}'::uuid[],
  is_active boolean NOT NULL DEFAULT true,
  sort_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT pricing_blocks_pkey PRIMARY KEY (id),
  CONSTRAINT pricing_blocks_schedule_id_fkey FOREIGN KEY (schedule_id) REFERENCES public.operating_schedules(id)
);
CREATE TABLE public.customers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  owner_id uuid NOT NULL,
  user_id uuid,
  full_name text NOT NULL,
  phone text NOT NULL,
  email text,
  notes text,
  total_visits integer NOT NULL DEFAULT 0,
  total_spent integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT customers_pkey PRIMARY KEY (id),
  CONSTRAINT customers_owner_id_fkey FOREIGN KEY (owner_id) REFERENCES public.owners(id),
  CONSTRAINT customers_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.bookings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  booking_number text NOT NULL UNIQUE,
  venue_id uuid NOT NULL,
  court_id uuid NOT NULL,
  customer_id uuid NOT NULL,
  booked_by uuid NOT NULL,
  date date NOT NULL,
  start_time time without time zone NOT NULL CHECK (EXTRACT(minute FROM start_time) = ANY (ARRAY[0::numeric, 30::numeric])),
  end_time time without time zone NOT NULL,
  duration_minutes integer NOT NULL CHECK (duration_minutes > 0 AND (duration_minutes % 60) = 0),
  base_amount integer NOT NULL,
  discount integer NOT NULL DEFAULT 0,
  final_amount integer NOT NULL,
  advance integer NOT NULL DEFAULT 0,
  pending integer NOT NULL DEFAULT 0,
  status USER-DEFINED NOT NULL DEFAULT 'upcoming'::booking_status,
  payment_status USER-DEFINED NOT NULL DEFAULT 'pending'::booking_payment_status,
  payment_mode USER-DEFINED,
  source USER-DEFINED NOT NULL DEFAULT 'offline'::booking_source,
  slot_type USER-DEFINED NOT NULL DEFAULT 'booked'::slot_type,
  is_force_booked boolean NOT NULL DEFAULT false,
  notes text,
  payment_notes text,
  whatsapp_sent boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT bookings_pkey PRIMARY KEY (id),
  CONSTRAINT bookings_venue_id_fkey FOREIGN KEY (venue_id) REFERENCES public.venues(id),
  CONSTRAINT bookings_court_id_fkey FOREIGN KEY (court_id) REFERENCES public.courts(id),
  CONSTRAINT bookings_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT bookings_booked_by_fkey FOREIGN KEY (booked_by) REFERENCES auth.users(id)
);
CREATE TABLE public.membership_slots (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  venue_id uuid NOT NULL,
  name text NOT NULL,
  playing_days ARRAY NOT NULL,
  start_time time without time zone NOT NULL,
  end_time time without time zone NOT NULL,
  skill_level USER-DEFINED NOT NULL DEFAULT 'intermediate'::skill_level,
  monthly_fee integer NOT NULL,
  capacity integer NOT NULL CHECK (capacity > 0),
  guest_play_fee integer NOT NULL DEFAULT 0,
  allow_guest_play boolean NOT NULL DEFAULT false,
  billing_day integer NOT NULL DEFAULT 1,
  is_published boolean NOT NULL DEFAULT false,
  is_recruiting boolean NOT NULL DEFAULT true,
  court_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT membership_slots_pkey PRIMARY KEY (id),
  CONSTRAINT membership_slots_venue_id_fkey FOREIGN KEY (venue_id) REFERENCES public.venues(id),
  CONSTRAINT membership_slots_court_id_fkey FOREIGN KEY (court_id) REFERENCES public.courts(id)
);
CREATE TABLE public.membership_slot_releases (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slot_id uuid NOT NULL,
  release_date date NOT NULL,
  released_by uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT membership_slot_releases_pkey PRIMARY KEY (id),
  CONSTRAINT membership_slot_releases_slot_id_fkey FOREIGN KEY (slot_id) REFERENCES public.membership_slots(id),
  CONSTRAINT membership_slot_releases_released_by_fkey FOREIGN KEY (released_by) REFERENCES auth.users(id)
);
CREATE TABLE public.members (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slot_id uuid NOT NULL,
  customer_id uuid NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  join_date date NOT NULL DEFAULT CURRENT_DATE,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT members_pkey PRIMARY KEY (id),
  CONSTRAINT members_slot_id_fkey FOREIGN KEY (slot_id) REFERENCES public.membership_slots(id),
  CONSTRAINT members_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.membership_applications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slot_id uuid NOT NULL,
  applicant_name text NOT NULL,
  phone text NOT NULL,
  photo_url text,
  skill_level USER-DEFINED,
  experience text,
  preferred_days ARRAY,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::application_status,
  reviewed_at timestamp with time zone,
  reviewed_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT membership_applications_pkey PRIMARY KEY (id),
  CONSTRAINT membership_applications_slot_id_fkey FOREIGN KEY (slot_id) REFERENCES public.membership_slots(id),
  CONSTRAINT membership_applications_reviewed_by_fkey FOREIGN KEY (reviewed_by) REFERENCES auth.users(id)
);
CREATE TABLE public.guest_plays (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  slot_id uuid NOT NULL,
  application_id uuid,
  player_name text NOT NULL,
  phone text NOT NULL,
  scheduled_date date NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'upcoming'::guest_play_status,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT guest_plays_pkey PRIMARY KEY (id),
  CONSTRAINT guest_plays_slot_id_fkey FOREIGN KEY (slot_id) REFERENCES public.membership_slots(id),
  CONSTRAINT guest_plays_application_id_fkey FOREIGN KEY (application_id) REFERENCES public.membership_applications(id)
);
CREATE TABLE public.membership_payments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  member_id uuid NOT NULL,
  slot_id uuid NOT NULL,
  amount integer NOT NULL,
  billing_period date NOT NULL,
  due_date date NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'due'::membership_pay_status,
  payment_mode USER-DEFINED,
  paid_on date,
  receipt_url text,
  notes text,
  recorded_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT membership_payments_pkey PRIMARY KEY (id),
  CONSTRAINT membership_payments_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.members(id),
  CONSTRAINT membership_payments_slot_id_fkey FOREIGN KEY (slot_id) REFERENCES public.membership_slots(id),
  CONSTRAINT membership_payments_recorded_by_fkey FOREIGN KEY (recorded_by) REFERENCES auth.users(id)
);